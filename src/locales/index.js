/**
 * @fileoverview Exports all locale objects.
 * Dependencies: locale files.
 * Used by: locale.js (or any module that needs direct access to locale data).
 */

import en from './en.js';
import es from './es.js';

export default {
  en,
  es,
};